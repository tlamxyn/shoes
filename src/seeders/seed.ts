import { users } from "./seed/user"
import { permissions } from "./seed/permission"
import { devices } from "./seed/device"
import { producttype } from "./seed/producttype"

export const seed = {
    users, 
    permissions, 
    devices,
    producttype
}